export interface PayrollApprovalHtmlProps {
    name: string;
    month: string;
    companyName: string;
    url: string;
}
export declare const payrollApprovalHtml: ({ name, month, companyName, url, }: PayrollApprovalHtmlProps) => string;
